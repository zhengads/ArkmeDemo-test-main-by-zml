即我「安排」模块迭代规划
根据原始需求口述，将该模块的开发拆解为以下遵循“最小可用、小步快跑”原则的 Task 列表。前置 Task 注重交互细节与用户体验（解压、优雅），后续 Task 逐个击破 AI 智能化场景。

Proposed Changes (整体 Task 拆解路线)
Task 1: 基础框架与手动闭环 (UI/UX 核心) - [已完成]
目标：搭建「安排」模块的基础 UI 框架，实现最基础的手动创建与状态流转，奠定优雅、解压的视觉与交互基调。

模块入口与主页：在底部导航新增「安排」入口，设计一个无压力的安排列表页（区分待办、已完成、以后再说）。
手动创建：提供极简的手动输入交互，支持用户自己创建那些 AI 难以识别的隐喻事项（如“~~”代表游泳）。
完成机制：优雅的手动完成交互（如平滑的划线动画、打勾微动效）。
“以后再说”机制 (解压设计)：对列表项支持右滑或长按，移动到“以后再说”状态，取代传统的过期标红提醒，减轻用户的焦虑感。
Task 2: AI 大模型 API 绑定基建 - [进行中]
目标：提供底层大模型能力基建，为后续所有智能化场景提供基础。

设置页拓展：在设置中新增“AI API 配置”项，允许用户输入自己的 API Key（如 OpenAI / 智谱等），消耗自己的 Token。
调用封装：将 API 配置安全存储在本地（LocalStorage），并封装统一的对话请求 Service 工具类。
Task 3: AI 场景一 - 单人对话内的单条意图识别
目标：跑通最基础的 AI 自动创建安排链路。

意图提取：在发给自己的单人对话（如快记）中，异步调用 AI 识别包含时间、地点、事件的安排内容（如“后天去一趟医院”）。
静默创建：识别成功后在「安排」模块自动生成一条对应记录，并在对话卡片旁给予轻量级的反馈 UI。
Task 4: AI 场景二 - 智能状态流转
目标：AI 能够根据后续对话，自动变更已有安排的状态，形成智能闭环。

上下文比对：当用户发送如“今天上午去医院体检了”的消息时，AI 自动在后台比对未完成的安排。
自动标记完成：匹配成功后，自动将对应安排变更为“已完成”，并给用户相应的 UI 提示。
Task 5: AI 场景三 - 多人聊天与连续上下文的归集
目标：处理复杂的对话流和人物关系。

连续对话识别：识别跨越多条消息的安排项（如对象分多条消息说“带A、带B、带C”），合并生成为单条安排。
多消息归集展示：在安排的详情页中，将触发该安排的所有相关对话上下文内嵌展示，方便回溯。
跨对象分配：在私聊中识别出属于不同人的任务（如给自己生成“帮xxx带早餐”，给对方生成“yy帮自己带早餐”）。
Task 6: 进阶体验 - 日历视图与 AI 代办分级
目标：提供全局的时间感知，并探索更高级的执行机制。

日历总览视图：在「安排」模块增加日历模式，直观展示日程（时间段）和任务（截止时间），让用户有全局总览之感。
执行分级处理机制：根据 AI 判断，在 UI 上区分“纯人工执行”、“AI 辅助部分执行”和“AI 全自动执行”的安排项，并分别提供对应的交互入口。
